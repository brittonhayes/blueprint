/**
 * Silver-only ink.
 *
 * Every shape renders its own inline silver SVG/HTML from these constants —
 * there is no palette or color picker. This is the single source of truth for
 * shape rendering and PNG export.
 */
export const INK_COLOR = '#c6ccd1'
export const INK_COLOR_SEMI = 'rgba(198, 204, 209, 0.16)'
export const INK_COLOR_FAINT = 'rgba(198, 204, 209, 0.5)'
